<div class="navbar navbar-static-top">
  <div class="navbar-inner">
    <div class="container">

      <!-- this a is for mobile and other non-desktop devices -->
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>

      <a class="brand" href="/">Messias</a>
      <div class="nav-collapse collapse">
        <% if user_signed_in? %>
          <ul class="nav">
            <%= nav_link "Dashboard", dashboard_path %>
            <%= nav_link "Användare", users_path %>
            <%= nav_link "Fakturor", invoices_path %>
            <%= nav_link "Kunder", customers_path %>
            <%= nav_link "Lager", warehouses_path %>
            <%= nav_link "Produkter", products_path %>
            <%= nav_link "Statistik", statistics_index_path %>
          </ul>

          <ul class="nav pull-right">
            <li class="dropdown">
              <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                <%= settings_icon %>
                <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to current_user.name, edit_user_path(current_user) %></li>
                <li><%= link_to "logout", destroy_user_session_path, :method => :delete %></li>
              </ul>
            </li>
          </ul>

        <% else %>
          <ul class="nav">
            <li><%= link_to 'Om', about_path %></li>
            <li><%= link_to 'Login', new_user_session_path %></li>
          </ul>
        <% end %>
      </div>

    </div>
  </div>
</div>
