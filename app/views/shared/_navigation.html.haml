.navbar.navbar-default
  .container-fluid
    .navbar-header
      %button.navbar-toggle{'data-toggle' => 'collapse', 'data-target' => '#navbar-collapses'}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      =link_to 'Emmy', '/', class: 'navbar-brand'

    #navbar-collapse.collapse.navbar-collapse
      - if user_signed_in?
        %ul.nav.navbar-nav
          = nav_link "Dashboard", dashboard_path
          = nav_link "Användare", users_path
          = nav_link "Fakturor", invoices_path
          = nav_link "Kunder", customers_path
          = nav_link "Lager", warehouses_path
          = nav_link "Produkter", products_path
          = nav_link "Statistik", statistics_index_path
        %ul.nav.navbar-nav.navbar-right
          %li.dropdown
            %a.dropdown-toggle{'data-toggle' => 'dropdown', 'href' => '#'}
              = current_user.name
              %b.caret
            %ul.dropdown-menu
              %li=link_to 'Profile', edit_user_path(current_user)
              %li=link_to 'Logout', destroy_user_session_path, method: :delete

      - else
        %ul.nav.navbar-nav
          %li
            =link_to 'About', about_path
          %li
            =link_to 'Login', new_user_session_path
