= simple_form_for(@default_code, wrapper: :horizontal_form, html: {class: 'form-horizontal'}) do |f|
  = f.error_notification
  = f.input :code
  = f.input :text

  .form-button-group
    .actions
      = f.submit class: 'btn btn-primary'


